overwrite: true
schema: 'src/graphql/schema/schema.graphql'
generates:
    src/types/generated/graphql.ts:
        plugins:
            - typescript
            - typescript-mongodb
        config:
            - avoidOptionals: true
